// Name: Bobby Singh Dhillon
// CruzID: bosdhill
// ID number: 1594478
// Assignment Name: Programming Assignment 5 -- Graph ADT
// Description: 
//-----------------------------------------------------------------------------
// GraphTest.c
// Test file for Graph.c and Graph.h
//-----------------------------------------------------------------------------

#include <stdio.h>
#include "Graph.h"

int main(int argc, char* argv[]){
    // Graph G = newGraph(5); 

    // addArc(1,2); 
    // addArc(3,4); 
    // addArc(5,1); 

    // List L = newList(); 

    // for (int i = 1; i <= 5; i++){
    //   append(L,i); 
    // }

    // DFS(G,S); 

    // printGraph(stdout,G); 
    // printList(stdout,S);

    // // tranpose once
    // Graph tG = tranpose(G); 

    // printGraph(tG); 

    // // tranpose again
    // tG = tranpose(tG);
    // G = copyGraph(G); 

    // DFS(tG); 
    // DFS(G); 

    // for (int i = 1; i <= 5; i++){
    //   if (getParent(tG) != getParent(G))
    //       printf("false!\n"); 
    // }

    // freeGraph(&G); 
    // G = newGraph(10); 

    // if (getSize(G) != 0)
    //   printf("false!\n"); 
    // addArc(A, 4, 1);
    // addArc(A, 4, 2);
    // addArc(A, 4, 3);
    // addArc(A, 1, 5);
    // addArc(A, 1, 5);
    // if (getSize(G) != 5)
    //   printf("false!\n"); 
    // for (int i = 1; i <= 10; i++) {
    // append(L, i);
    // }
    // DFS(G, L);
    // if (getSize(G) != 5)
    //   printf("false!\n"); 
    // addArc(A, 55, 1);
    // if (getSize(G) != 6)
    //   printf("false!\n"); 
    // printf("TRUE!\n"); 

    // freeGraph(&tG);
    // freeGraph(&G); 
    // freeList(&S); 
   return(0); 
}